"use strict";
class Book {
    constructor(isbn, title, author, availability, issuedDate, handoverDate) {
        this.isbn = isbn;
        this.title = title;
        this.author = author;
        this.availability = availability;
        this.issuedDate = issuedDate;
        this.handoverDate = handoverDate;
    }
}
